    <div class="panel panel-default">
        <div class="panel-heading">
            <p style="display: -webkit-inline-box;">{{body.title}}</p>
            <span class="badge badge-info pull-right">#3</span>
        </div>
        <div class="panel-body">
            <p><b>Cheater Username -</b> {{body.hacker_username}}</p>
            <p><b>Server -</b> {{body.server}}</p>
            <p><b>Proof -</b> {{body.proof}}</p>
        </div>
    </div>
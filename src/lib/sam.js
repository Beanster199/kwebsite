module.exports = {
/*
    // /If user is not logged in redirect to /logi
    isLoggedIn(req, res, next) {
        if (req.isAuthenticated()){
            return next();
        }
       return res.redirect('/login');
    } */
}